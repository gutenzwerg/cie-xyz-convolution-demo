# cie-xyz-convolution-demo
An interactive HTML/JavaScript demonstration that visualises the complete chain:  Reflectance curve → Illuminant → Stimulus curve Φ(λ) → Convolution with the CIE colour matching functions → CIE XYZ
